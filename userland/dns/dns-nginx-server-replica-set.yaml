kind: ReplicaSet
name: dns-nginx-server-pod
volume:
  - $/dns/conf/:/etc/nginx/conf.d/
cpu: 1
mem: 1500m
metadata:
  labels:
    app: dns-nginx-server
spec:
  replicas: 1
containers:
  - name: nginx
    image: nginx
    command:
    resource:
      memory: 500m
      cpu: 1
    port: 80